import styled from 'styled-components';

const StyledTitle = styled.div`
    font-size: 3.5em;
    font-weight: bold;
    text-align: center;
    margin: 0;
    padding: 0;
`;

export default StyledTitle;