import styled from 'styled-components'

const StyledScrollArrow = styled.div`
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: center;
    width: 10vw;

.arrow {
    width: 3em;
    height: 3em;
    margin-bottom: 4em;
    background-color: #f2f0f0;
}
`;

export default StyledScrollArrow;